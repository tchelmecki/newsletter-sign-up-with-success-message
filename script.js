const email = document.querySelector('input');
const emailCheck = document.querySelector('.email-check');

email.addEventListener("input",(e)=>{
    try {
        
    } catch (error) {
        
    }
    // // emailCheck.textContent = e.target.value;
    // console.log(e);
})
